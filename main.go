package main

import (
	"itinerary-tool/itinerary"
	// "itinerary-tool" is the module name defined in go.mod
	// "itinerary" is the package inside the "itinerary-tool" module
)

func main() {
	itinerary.Starting() 
	// Calling the function from the itinerary package in start.go file
}